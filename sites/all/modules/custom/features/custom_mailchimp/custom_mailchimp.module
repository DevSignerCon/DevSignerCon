<?php/** * Implements hook_menu(). */function custom_mailchimp_menu() {     $items = array();     $items['i/mailchimp-signup/%'] = array(          'title' => 'MailChimp Signup',          'description' => 'Intercept Mailchimp send process',          'page callback' => 'mailchimp_send',          'access arguments' => array('access content'),          'page arguments' => array(2),          'type' => MENU_CALLBACK,     );     return $items;}//Newsletter/Mailchimp callbackfunction mailchimp_send($email){     //$success  = $q->listSubscribe($list_id, $email, $merge_vars, 'html', TRUE, TRUE);     $merge_vars;     $list_id  = '1494e75afa';     $q        = mailchimp_get_api_object();     mailchimp_subscribe_process($list_id, $email);}